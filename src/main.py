__author__ = 'Kern'

# pipeline:
# image preprocess => image feature extraction => combine different features together => feature data preprocess(cross validation, nomorlize) =>. training regression model via feature vector => evaluation